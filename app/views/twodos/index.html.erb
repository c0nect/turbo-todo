<div class="mx-auto w-1/2 ">
  <h1 class="text-2xl font-bold text-gray-900 sm:text-3xl py-1">Todo app</h1>
  <p class="text-1xl font-light">
    <% if user_signed_in? %>
      Welcome, <%= current_user.email + '...' %>
      <%= button_to(
        'do u want logout?',
        destroy_user_session_path,
        method: :delete,
        class: 'hover:text-gray-600 text-gray-800 font-semibold'
      ) %>
    <% else %>
      You no ared logged,
      <%= link_to(
        'Login now 🥲',
        new_user_session_path,
        class: 'hover:text-gray-600 text-gray-800 font-semibold'
      ) %>
    <% end %>
  </p>
  
  <div class="w-full max-w-2xl bg-gray-100 py-8 px-4 border border-gray-200 rounded shadow-sm">
    <div class="py-2 px-4">
      <%= render "form", twodo: Twodo.new %>
    </div>
    <ul id="twodos">
      <%= render @twodos %>
    </ul>
  </div>
</div>